@model WebApplication4.Models.Rezervacija

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Rezervacija</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
           
            <div class="form-group">
                <label asp-for="SobaId" class="control-label"></label>
                <select asp-for="SobaId" class="form-control" asp-items="ViewBag.SobaId"></select>
                <span asp-validation-for="SobaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="VrijemeDolaska" class="control-label"></label>
                <input asp-for="VrijemeDolaska" class="form-control" />
                <span asp-validation-for="VrijemeDolaska" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="VrijemeOdlaska" class="control-label"></label>
                <input asp-for="VrijemeOdlaska" class="form-control" />
                <span asp-validation-for="VrijemeOdlaska" class="text-danger"></span>
            </div>
          
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
